generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Soul {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  content     String
  fileName    String
  createdAt   DateTime @default(now())
}
