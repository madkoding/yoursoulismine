generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL", "file:./.vercel/sqlite.db")
}

model Soul {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  content     String
  fileName    String
  createdAt   DateTime @default(now())
}
